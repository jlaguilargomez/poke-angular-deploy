import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PokemonDetailComponent } from './pokemon-detail.component';
import { PokemonCardComponent } from './pokemon-card/pokemon-card.component';
import { PokemonMapComponent } from './pokemon-map/pokemon-map.component';
import { ActivatedRoute } from '@angular/router';

describe('PokemonDetailComponent', () => {
	// beforeEach(() => {
	// 	TestBed.configureTestingModule({
	// 		declarations: [PokemonDetailComponent, PokemonCardComponent, PokemonMapComponent],
	// 		providers: [ActivatedRoute]
	// 	}).compileComponents();
	// });
	// it('Component Header works!', () => {
	// 	let fixture = TestBed.createComponent(PokemonDetailComponent);
	// 	let component = fixture.debugElement.componentInstance;
	// 	expect(component).toBeTruthy();
	// });
});
